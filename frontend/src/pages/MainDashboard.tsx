import { Container } from "@mui/material";

export const MainDashboard = () => {
  return (
    <Container maxWidth="lg" sx={{ py: 4 }}>
      TBD, this will be the main dashboard containing information about all the
      aspects of fitness that i track, Weight, Strength, and Cardio. It will be
      a high level overview of all the data and progress. It will also contain
      links to the different sections of the app.
    </Container>
  );
};
